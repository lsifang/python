# 1、通过import直接导入包时并不会自动导入所有的模块，需要借助from
# 2、包必须包含__init__.py文件，并且在包被导入时会自动的运行以及导入
# 3、通过import xxx.xxx.xxx as xxx可以简化代码，避免冗余
# 4、｛通过import直接导入包时并不会自动导入所有的模块，需要借助from｝  可以借助__init__模块事先导入包里的模块（需要指明绝对路径那一个包下面的哪一个模块）
